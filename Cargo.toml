[workspace]
members = ["crates/core"]
resolver = "2"

# Single source of truth for the crate version (kept in step with package.json,
# which the extension manifest derives from).
[workspace.package]
version = "1.0.0"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
